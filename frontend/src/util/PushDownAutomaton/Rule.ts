type Rule = {
	currentState: string;
	currentInput: string;
	currentVisibleStacks: string;
	newState: string;
	processStackReplacement: string;
	resultStackReplacement: string;
};

export default Rule;
